# topics/

One folder per Special Topic: `topics/<id>/README.md` (the brief) and `topics/<id>/source/`
(drop folder for raw material — gitignored, never published). Created by
`npm run new-topic -- <id>`; see «Adding a topic» in the main README.
